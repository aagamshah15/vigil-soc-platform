services:
  web:
    build:
      context: ./services/web
    platform: linux/arm64
    container_name: soc-command-center
    depends_on:
      api:
        condition: service_started
    ports:
      - "8600:80"
    restart: unless-stopped
